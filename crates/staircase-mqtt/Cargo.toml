[package]
name = "staircase-mqtt"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "MQTT client protocol driver for Staircase (implementation pending)"

[dependencies]
staircase-core = { workspace = true }
